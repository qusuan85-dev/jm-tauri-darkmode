# 更新说明

## v0.1.25+darkmode4（当前版本）

> 版本名 `0.1.25+darkmode4` · versionCode `1784900003` · 包名 `com.aa.jm`
> 下载：见 [Releases](https://github.com/qusuan85-dev/jm-tauri-darkmode/releases)

### 修复

**从收藏夹点进本子，返回后掉回默认收藏夹**

- 原因：收藏页的「当前收藏夹 / 页码 / 排序」只是页面内部状态，**不在地址栏里**；进本子时记录的返回地址是裸的 `/home/favorites`，回来时页面重新挂载，三项一起复位
- 现在这三项都写进地址（形如 `/home/favorites?folder=2&page=3&sort=mp`），返回即还原；刷新页面、前进/后退也保持一致
- 顺带记住**滚动位置**：返回时回到刚才看到的那一段

### 新增

**收藏夹批量整理**（列表右上角「批量整理」）

- 进入后点击封面或标题即选中，选中的条目带高亮描边（此时不会跳转详情）
- 选择方式：**全选本页** / **全选该收藏夹（所有页）** / 反选本页 / 清空选择
- **批量移动到指定收藏夹**：面板里列出所有收藏夹，点一个即开始；当前所在的收藏夹会标注「（当前）」；面板里也能直接**新建收藏夹**再移进去
- **批量取消收藏**
- 处理过程显示「正在处理 n/N」，结束后给出成功/失败数量与失败明细（最多 3 条）

### 安装

- 直接覆盖安装即可，**登录态 / 收藏 / 阅读进度都不会丢**
- 校验值：
  - `jm-0.1.25-darkmode4-universal.apk`（4 种 ABI）— `47bf44f08823e33e0b90b5ed18b647db78f05c48698a1deaa3a4b90f71a769e7`
  - `jm-0.1.25-darkmode4-arm64.apk`（仅 arm64）— `d6b52f6fdbcac02ebb6ed949db305d24ac510a5934c36495b2929955ce976349`

### 说明

- JM 的收藏接口**没有批量版本**（移动与取消收藏都是针对单个本子），所以批量是**逐条串行**执行：更稳、不容易触发风控，进度与失败归因也更清楚
- 批量模式下「排序 / 收藏夹」两个下拉会暂时禁用 —— 否则选到一半列表被换掉，选择就失去意义了

---

## v0.1.25+darkmode3（上一版）

> 版本名 `0.1.25+darkmode3` · versionCode `1784900002` · 包名 `com.aa.jm`
> 下载：见 [Releases](https://github.com/qusuan85-dev/jm-tauri-darkmode/releases)

### 新增

**签到页面（每日打卡）**

- 入口：侧边栏「签到」；手机端是底部导航的**第 6 个图标**（首页 / 阅读 / **签到** / 排行 / 搜索 / 设置）
- 显示今日状态（今天还没签到 / 今天已经签过到了）与连续进度，接口返回连续天数时也一并显示
- **立即签到**按钮：签到成功后直接给出拿到多少 ——「**+40 金币**」「**+40 经验**」，数值从服务端返回里解析；今天已签到时只给提示，不会伪造奖励
- **本月打卡日历**：已签到 / 未签到 / 未来 / 今天（高亮）/ 活动双倍日（★）
- **连续奖励**一览：满 3 天、满 7 天的金币与经验
- 「自动打卡」开关从登录页移到签到页（登录页少一个开关）；App 启动时的自动打卡行为不变

### 安装

- 直接覆盖安装即可，**登录态 / 收藏 / 阅读进度都不会丢**
- 校验值：
  - `jm-0.1.25-darkmode3-universal.apk`（4 种 ABI）— `cd15436abece364bd223d605be83dbb3a40b005208d2499d5318b22e15b4d1e4`
  - `jm-0.1.25-darkmode3-arm64.apk`（仅 arm64）— `c4aeeea0392796423bbbaa8447bd6dc1a2afadfd563a98a752a8443c0a539d9b`

### 小提示

- 奖励文案按 `Jcoin:<数字> EXP:<数字>` 解析（全角冒号、空格、大小写都容错）。如果服务端的文案格式不同，界面会把**服务端原文**显示出来（「服务端返回：…」那一行），把那一行发我即可精确适配。

---

## v0.1.25+darkmode2（上一版）

> 版本名 `0.1.25+darkmode2` · versionCode `1784900001` · 包名 `com.aa.jm`
> 下载：见 [Releases](https://github.com/qusuan85-dev/jm-tauri-darkmode/releases)

### 新增

**1. 收藏时可以指定收藏夹**

- 详情页点「收藏」→ 弹出收藏夹面板，选择要放进哪个收藏夹（含「默认收藏夹」）
- 已经收藏过的本子，按钮会变成「已收藏 · 移动」，可以移动到别的收藏夹，也可以在同一面板里取消收藏
- 面板里可以直接新建收藏夹，建完立刻可选
- 说明：移动端接口不支持「收藏时直接指定收藏夹」，所以实现上是「先收藏、再移动」两步，效果一致

**2. 新增「已缓存」页面，一眼看到缓存了哪些本子**

- 入口：侧边导航「已缓存」；手机端在底部「阅读」菜单里
- 每行显示：封面、标题、作者、占用大小、文件数、**已存话数**、最近缓存时间
- 每行可操作：**阅读**（离线可用，自动接上次的章节与页码）、点标题/封面进详情、**删除**单个本子
- 支持按标题或 AID 筛选；按「最近缓存 / 占用大小 / 标题」排序；列表与卡片两种视图
- 数据全部来自本地缓存，**断网也能打开这个页面**
- 之前只藏在「设置 → 缓存详情」里，而且只有 AID，没有封面和标题

**3. 封面上的「已缓存」角标**

- 首页、搜索、收藏（在线）、浏览记录、分类排行、详情页的封面右上角，已缓存的本子会显示「已缓存」小角标
- 缓存或删除之后角标会自动更新，不需要重启

### 移除

**「本地收藏」功能整体移除**

- 侧边导航、手机端「阅读」菜单里的入口，以及详情页、阅读页菜单里的「本地收藏」按钮全部去掉
- 设备上原本地收藏的数据文件**保留、不删除**，也不影响任何现有功能

### 安装

- 直接覆盖安装即可，**登录态 / 收藏 / 阅读进度都不会丢**
- 版本号高于上一版，无需卸载；签名与原版一致
- 校验值：
  - `jm-0.1.25-darkmode2-universal.apk`（4 种 ABI）— `a34404e814924fd6252395a9ac5116cff388c37be94a9e1741d50cb2c75ee835`
  - `jm-0.1.25-darkmode2-arm64.apk`（仅 arm64）— `9bcdb8a61114269d940e0bfff524ca16a4337270d5bf68e75b0e6b1e18e45249`

### 小提示

- 「移动到收藏夹」这个接口是上游代码里写好了但**从未接到界面上**的，服务端路由确认存在（未登录会返回 401，而不是"路由不存在"）。如果移动失败，界面会弹出具体错误，反馈时把那一行发出来即可。

---

## v0.1.25+darkmode（上一版）

> versionCode `1784900000`

- **应用图标**更换（含 Android 8+ 自适应图标）
- 修复：底部导航「阅读」按钮点了没反应（弹出菜单被 `overflow-hidden` 裁掉了）
- 修复：导出图片 / PDF **全部失败**（章节接口返回的是裸文件名，Rust 侧没拼 CDN 前缀就直接请求）
- 修复：导出的图片是**错位条带**（解密分块数用了带扩展名的文件名去算，而它必须用不含扩展名的）
- 修复：中文标题被过滤成 `unknown` 导致不同漫画互相覆盖、超长标题导致导出失败、单章失败拖垮整本导出等问题

---

## 更早的改动（同一个版本号 `0.1.25+darkmode` 内）

| 批次 | 内容 |
| --- | --- |
| 暗色模式 | 浅色 / 深色 / 跟随系统三档，全站颜色改为变量驱动；新增设置页「外观」卡片 |
| 搜索增强 | 可指定每次加载条数（10/20/30/50）；划到最底部自动加载下一批；切到别的页面再回来，搜索条件、结果与滚动位置都保留 |
| 界面观感 | 全站圆角体系（卡片 16px、控件 12px）；底部导航与浮层使用液态玻璃；7 套主题色可切换 |
| 导出到目录 | 新增「导出」面板：可导出解密原图、每章一个 PDF、整本合并 PDF；可选默认目录或系统文件夹选择器 |
